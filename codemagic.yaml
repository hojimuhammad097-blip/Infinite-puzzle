workflows:
  android-build:
    name: Android Build
    environment:
      flutter: 3.22.3
    scripts:
      - flutter pub get
      - flutter build apk ---releasel
      artifacts:
        - build/app/outputs/flutter-apk/*.apk
